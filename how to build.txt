在快速启动栏，启动Visual Studio Command Prompt (2010)
初始化编译环境，C:\pb190t_R3\IDE\tools 指令： C:\pb190t_R3\IDE\tools\pbenv.bat UNICODE
进入需要编译的模块：例如system：
cd C:\pb190t_R3\IDE\system ----------》make 等待编译完成即可。

在编译runtime后，还需要编译一次build才能在VS里面正确链接起来。

cd到这个目录就可以直接运行编译。
C:。。。\IDE\tools\makeide.bat


单独编译：
初始化编译指令： C:\PB2019R3C\IDE\tools\pbenv.bat UNICODE
全部编译：
英文版指令：C:\PB2019R3C\IDE\tools\makeide.bat 
日文版指令：C:\PB2019R3C\IDE\tools\makeide.bat jpn

将PBdownload下来后，编译完成，需要在C:\。。。\IDE\build\unicode\PB190.exe 将图标拉出来。

并且在Configuration Properties-->Debugging中将Command的调试程序链接进去。



修改makex64脚本
C:\PB2019R3C\IDE\build\makex64.bat

set PSX64=TRUE
set pbstatus=
set INFORMIXDIR=C:\Program Files\IBM Informix Client SDK
cd ..\tools
call pbenv.bat UNICODE %PSRCNAME%


		TCHAR szBuf[512] = {0};
		wsprintf(szBuf, L"CMenuView::Show::菜单已显示==========%d=============\r",message);
		OutputDebugString(szBuf);


		TCHAR szText[255]={0};
		wsprintf(szText, _T("hWnd: %x uMsg: %x wParam: %d lParam: %d ====\n"), hWnd, uMsg, wParam, lParam);
		OutputDebugString(szText);


ob_global_search_in_liblist

// Release版本编译方法
UpdateEnvForRelease.bat   ide\tools路径下
makeide.bat 
